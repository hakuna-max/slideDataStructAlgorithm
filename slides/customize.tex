\usepackage{xcolor} % needed for \textcolor


\usepackage{amsfonts,amsmath,oldgerm}
% \usepackage{clrscode4e}
\usepackage[fontset = fandol]{ctex}


\usepackage{import}

\usepackage{minted}

\setminted{
    linenos = true,
    autogobble = true,
    breaklines = true
}

\setmintedinline{
    % bgcolor = gray!20,
    fontsize = \footnotesize,
    escapeinside=||,
    mathescape = true,
}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,       % 设置为 true 以便使用颜色区分链接，而不是使用带框的链接；如果您喜欢带框的链接，将此设置为 false
    linkcolor=blue,        % 内部链接的颜色，例如目录中的条目
    filecolor=magenta,     % 文件链接的颜色
    urlcolor=cyan,         % URL 链接的颜色
    citecolor=green,       % 文献引用链接的颜色
    pdftitle={Document Title},    % PDF 文档的标题
    pdfauthor={Author Name}       % PDF 文档的作者
}
\usepackage{tikz}

\usepackage{amssymb}
\usepackage{booktabs}
% 设置所有的table字号为footnotesize
% \let\oldtable\table
% \let\endoldtable\endtable

% \renewenvironment{table}[1][htbp]
% {\oldtable[#1]\footnotesize}
% {\endoldtable}


\usetheme{sintef}

\newcommand{\testcolor}[1]{\colorbox{#1}{\textcolor{#1}{test}}~\texttt{#1}}

\usefonttheme{serif}

\titlebackground{images/background.png}

\newcommand{\hrefcol}[2]{\textcolor{cyan}{\href{#1}{#2}}}


% style of section presented in the table of contents
\setbeamertemplate{section in toc}{$\blacktriangleright$~\inserttocsection}

% style of subsection presented in the table of contents
\setbeamertemplate{subsection in toc}{}
% \setbeamertemplate{subsection in toc}{\footnotesize\hspace{1.2 em}$\blacktriangleright$~\inserttocsubsection}

% automate subtitle of each frame
% \makeatletter
% \pretocmd\beamer@checkframetitle{\framesubtitle{\thesection \, \secname}}
% \makeatother

% avoid numbering of frames that are breaked into multiply slides
\setbeamertemplate{frametitle continuation}{}

% at the beginning of section, add table of contents with the current section highlighted
\AtBeginSection[]
{
    \begingroup
    \setbeamertemplate{footline}{}
    \themecolor{blue}
    \begin{frame}{Table of Contents}
        \tableofcontents[currentsection]
    \end{frame}
    \endgroup
}


% % redefine \emph command
\let\oldemph\emph % save the old \emph command
\renewcommand{\emph}[1]{\textbf{\textit{\textcolor{maroon}{#1}}}}